import {ConfigStructure} from "./structure/config-structure";

const CONFIG: ConfigStructure = {
    serverUrl: "http://localhost:8082",
    date: {
        longFormat: "Do MMM YYYY",
        shortFormat: "MM/DD/YYYY"
    }
};

export default CONFIG;
